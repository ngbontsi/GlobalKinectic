# GlobalKinectic
on the project you will need to install git, maven,msql 
on msql name you database globalkinectic and create tabe called users and populate any information you want
when testing the application please run mvn springboot:run and you can use any tool for the client as it will respond to any localhost:8080 call
